import React from "react";

// Input: liked: boolean
// Output: onClick event

const CustomerIcon = () => {
  return (
    <i
      className="fa fa-address-card"
      aria-hidden="true"
      style={{ marginRight: 5 }}
    />
  );
};

export default CustomerIcon;
